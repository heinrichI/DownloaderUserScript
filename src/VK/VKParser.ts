export function Parse(input: string) 
{
    const start: string = "<a class=\"page_doc_icon page_doc_icon1\" href=\"\/";

    let index: number = input.indexOf(start);

    let res = input.substr(start[, length]);

    const numbers: string[] = [];
    var length = numbers.push(res); 
}